<div class="w-full bg-slate-600 text-white">
	<div class="container mx-auto flex flex-row justify-center space-x-12 uppercase py-2 px-4">
		{% for link in linklists.footer.links %}
			<a href="{{ link.url }}">{{ link.title }}</a>
		{% endfor %}
	</div>
</div>
<div class="w-full bg-slate-800 text-white">
	<div class="container mx-auto flex flex-row justify-center space-x-4 p-4">
		<p>Copyright @ This Store</p>
		<p>|</p>
		<p >Website By My Company</p>
	</div>
</div>